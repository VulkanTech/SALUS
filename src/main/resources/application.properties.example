spring.application.name=salus
server.port=3000
# Configura��o do Banco de Dados PostgreSQL
spring.datasource.url=jdbc:postgresql://localhost:5432/ubs_agendamento
spring.datasource.username=postgres
    spring.datasource.password=COLOQUE AQUI SUA SENHA DO POSTGRE

# Configura��o do JPA (Hibernate)
spring.jpa.hibernate.ddl-auto=update
# 'update': Atualiza o schema (cria/altera tabelas) automaticamente ao iniciar.
# �timo para desenvolvimento. Mude para 'validate' ou 'none' em produ��o.
spring.jpa.show-sql=true
# Mostra os comandos SQL gerados no console (�timo para debug)